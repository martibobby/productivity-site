



























/* BEFORE SAM
/*Adds new item to the to-do list
function newItem(){
    var ul = document.getElementById("tasks");
    var li = document.createElement("li");

    var dateul = document.getElementById("dates");
    var dateli = document.createElement("li");


    var task = document.getElementById("newtask").value;
    var tasktext = document.createTextNode(task);
    /*error checking for no input ??

    //create removebutton and onclick function to remove the list item
    var removebutton = document.createElement("button");
    removebutton.innerHTML = "X";
    removebutton.style.marginRight = "10px";
    removebutton.onclick = function() {ul.removeChild(li);};

     DATE VARIABLES
    //var datestring = document.getElementById("date").value;
    //var duedate = new Date(datestring);
    //var dateNode = document.createTextNode(datestring);
    //dateli = document.create

    //li.appendChild(removebutton); //add remove button to item
    //create description for list item
    li.appendChild(dateNode); //add input text to item

    //add date to date column
    dateul.appendChild(dateli);

    ul.appendChild(li);

    //reset input box
    document.getElementById("newtask").value = "";
};
*/